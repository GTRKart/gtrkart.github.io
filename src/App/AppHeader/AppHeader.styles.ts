import { styled } from "styled-components";

export const AppHeaderContainer = styled.header`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  z-index: 1;

  color: #fff;

  > * {
    padding: 32px;
  }
`;
